## 1. Implementation
- [x] 1.1 Update `src/app/invite/[uniqueToken]/page.tsx` generateMetadata.
  - Set `robots: { index: true, follow: true }`.
  - Ensure title and description are optimized for public view.
- [x] 1.2 Verify `src/app/layout.tsx` still has default `noindex` (safe fallback).
- [x] 1.3 Verify social previews still work.
